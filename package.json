{
  "name": "my-turborepo",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "author": "",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start:backend": "pnpm --filter api start",
    "start:frontend": "pnpm --filter web start -- -p 3001",
    "start": "pnpm start:backend",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev:frontend": "pnpm --filter web dev",
    "dev:backend": "pnpm --filter api dev",
    "check:backend": "pnpm --filter api lint && pnpm --filter api run format:check && pnpm --filter api run test",
    "check:frontend": "pnpm --filter web lint && prettier --write apps/web/**/*.{ts,tsx,js,jsx,json} && set ANALYZE=true && pnpm --filter web build",
    "check": "pnpm --filter web lint && prettier --write apps/web/**/*.{ts,tsx,js,jsx,json} && set ANALYZE=true && pnpm --filter web build && pnpm --filter api lint && pnpm --filter api run format:check && pnpm --filter api run build && pnpm --filter api run test",
    "check:fast": "pnpm --filter web lint && prettier --check apps/web/**/*.{ts,tsx,js,jsx,json} && pnpm --filter api lint && pnpm --filter api run format:check"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "concurrently": "^9.2.1",
    "prettier": "^3.6.2",
    "turbo": "^2.6.1"
  },
  "packageManager": "pnpm@8.15.5",
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "overrides": {
      "puppeteer": "^24.30.0",
      "unzipper": "^0.12.3",
      "glob": "^10.0.0",
      "rimraf": "^5.0.0"
    }
  },
  "dependencies": {
    "@tiptap/extension-link": "^3.11.0",
    "@tiptap/extension-underline": "^3.11.0",
    "@tiptap/react": "^3.11.0",
    "@tiptap/starter-kit": "^3.11.0"
  }
}
